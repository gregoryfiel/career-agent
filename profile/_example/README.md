# Example profile — fictional

**Ana Beatriz Moraes does not exist.** This folder is committed so the file format is visible in the
repository without exposing anyone's real career.

Your own profile lives in `profile/<your-name>/` and is gitignored. Run skill `00-onboarding` to
create it.
